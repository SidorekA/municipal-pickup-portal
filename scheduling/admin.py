# scheduling/admin.py
from django.contrib import admin
# from .models import PickupSchedule

# @admin.register(PickupSchedule)
# class PickupScheduleAdmin(admin.ModelAdmin):
#     list_display = ("location", "fraction", "weekday", "typical_pickup_time", "active")
#     list_filter = ("active", "weekday", "fraction")
#     search_fields = ("location__name", "fraction__name")